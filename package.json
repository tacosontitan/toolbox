{
  "name": "hazels-toolbox",
  "displayName": "Hazel's Toolbox",
  "description": "Just a collection of tools created by and for Hazel to just do her job on a day-to-day basis.",
  "publisher": "tacosontitan",
  "version": "0.0.3",
  "engines": {
    "vscode": "^1.98.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "configuration": {
      "title": "Hazel's Toolbox",
      "type": "object",
      "properties": {
        "pineappleCove.toolbox.azure.devops.useClassicUri": {
          "type": "boolean",
          "default": false,
          "description": "Does your organization use the classic URI format for Azure DevOps?"
        },
        "pineappleCove.toolbox.azure.devops.organization": {
          "type": "string",
          "default": "",
          "description": "What's the name of your Azure DevOps organization?"
        },
        "pineappleCove.toolbox.azure.devops.project": {
          "type": "string",
          "default": "",
          "description": "What's the project you're working with in Azure DevOps?"
        },
        "pineappleCove.toolbox.azure.devops.userDisplayName": {
          "type": "string",
          "default": "",
          "description": "What's your display name in Azure DevOps?"
        }
      }
    },
    "commands": [
      {
        "command": "pineappleCove.toolbox.azure.devops.createDefaultTasks",
        "title": "Hazel's Toolbox: Create Default Azure DevOps Tasks"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.98.0",
    "@typescript-eslint/eslint-plugin": "^8.25.0",
    "@typescript-eslint/parser": "^8.25.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "esbuild": "^0.25.0",
    "eslint": "^9.21.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "azure-devops-node-api": "^14.1.0"
  }
}
